global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: ticketgate-api
    static_configs:
      - targets:
          - host.docker.internal:5123
    metrics_path: /metrics
    scrape_interval: 10s
